# Phase 2: Travelog Client-Side Implementation (Full Detail)

이 단계에서는 서버/DB 없이 **클라이언트 UI와 인터랙션**을 완벽히 구현합니다. 모든 데이터는 `dummy data`를 사용하며, API 통신은 가짜 지연 시간(Promise)을 두어 처리합니다.

## Tech Stack & Rules

-   **Framework**: Next.js (App Router), Lucide React (Icons)
-   **UI/Styling**: Tailwind CSS, Shadcn UI, Framer Motion (Transitions)
-   **State**: `URL SearchParams`를 적극 활용하여 페이지 상태(포커스된 지점 등)를 관리합니다.

---

## Pages & Components Spec

### 1. `Dashboard` ("/dashboard") - [권한: Authorized]

-   **내 프로필**: 상단에 요약 정보 표시.
-   **내 여행 기록 리스트**:
-   각 리스트 요소는 독립된 컴포넌트로 분리.
-   요약 정보(제목, 날짜, 대표 이미지) 제공 및 `/travel/:id`로 연결되는 Link 포함.

### 2. `Travel` ("/travel/:id") - [권한: Public/Member]

이 페이지는 서비스의 핵심이며, 구성 요소 간의 데이터 연동이 중요합니다.

-   **TravelTitle (상단 고정 헤더)**
-   **좌측 묶음**: 제목 + 다녀간 국가들의 국기 아이콘 리스트 + 여행 기간.
-   **우측 묶음**: 좋아요 아이콘 및 개수 버튼 + '여행 슬라이드쇼 재생' 버튼 + 권한 표시 + '수정(멤버전용)' 버튼.

-   **GoogleMap (배경/메인)**
-   **초기 뷰**: 모든 마커가 한눈에 들어오도록 적절한 Zoom 레벨로 시작.
-   **마커**: 기본 핀 대신 해당 지점의 사진을 노출하며, 시간 순서대로 선(Polyline)으로 연결.
-   **강조**: 첫 번째 여행 지점(시작점)은 시각적으로 강조.
-   **인터랙션**: 특정 지점 클릭 시 해당 위치를 화면 중앙으로 이동(Pan)하고 Zoom-in. 상태는 URL Params로 관리.

-   **Sidebar (좌측)**
-   각 지점 컴포넌트(사진+위치+제목)를 시간 순으로 수직 나열.
-   내용이 길어질 경우 내부 스크롤 적용.
-   첫 번째 지점은 별도 강조 디자인 적용.

-   **DetailSidebar (우측 슬라이드)**
-   평소엔 숨겨져 있다가 지점 클릭(포커스) 시 등장.
-   상세 정보: 사진, 날짜, 시간, 제목, 상세 설명 표시.
-   **수정 버튼**: 권한이 있을 때만 펜 모양 아이콘 노출.
-   **이전/다음 버튼**: 지점 간 이동 가능. 단, 이전/다음 지점이 없을 경우 해당 버튼은 숨김 처리.

### 3. `Show` ("/travel/:id/show") - [권한: Public/Member]

-   **로딩 섹션**: 슬라이드쇼 생성 전, 아래 문구를 3초마다 번갈아 노출.
-   " [닉네임]님의 여행을 돌아보고 있어요!" ↔ "거의 다 완성됐어요!"

-   **메인 캐러셀**:
-   각 슬라이드는 `DetailSidebar`의 정보가 담긴 카드 형태.
-   중앙 컨텐츠는 확대, 좌우 다음 사진은 화면에 살짝 걸치는 3D Carousel 효과.

-   **기능**: 상단 혹은 하단에 '여행 지도로 돌아가기' 링크 버튼 배치.

### 4. `Upload` ("/travel/upload") - [권한: Authorized]

-   **상단 Progress Bar**: 4개 단계(Step)를 시각적으로 표시.
-   **단계별 캐러셀 전환**:
-   **1단계**: 여행 제목 입력 ("여행을 한 마디로 요약하자면?")
-   **2단계**: 사진 업로드 (최대 20장/장당 20MB). 메타데이터 분석 시뮬레이션 로딩 포함.
-   **3단계 (결과 안내)**: "N개 중 M개의 사진이 정보 부족으로 제외되었습니다." 문구 노출.
-   버튼: [네 알겠습니다] (4단계 이동) / [다시 업로드] (사진 초기화)

-   **4단계 (최종 로딩)**: 여러 문구가 지나간 후 "Travelog [제목]이 생성되었습니다" 메시지 및 '보러가기' 버튼 노출.

### 5. `Search` ("/search") - [권한: All]

-   **기능**: `searchParam`을 활용한 검색 및 필터링.
-   **UI**: 검색바 + 리스트 영역.
-   **로직**: `Dashboard`에서 사용한 리스트 컴포넌트를 재활용하며, **무한 스크롤(Infinite Scroll)**과 **최신순 정렬**을 구현.

---

## 추가 가이드

1. **Mock Data**: `src/lib/mocks.ts` 파일에 모든 페이지에서 사용할 더미 데이터를 구조화해서 먼저 작성해줘.
2. **Reusable UI**: `Sidebar`와 `Search`에서 쓰이는 리스트 아이템은 최대한 공통 컴포넌트로 만들어줘.
3. **URL 기반 상태**: `Travel` 페이지에서 지점 선택 상태는 반드시 `?spot=id`와 같은 쿼리 스트링으로 관리해서 새로고침해도 유지되게 해줘.
