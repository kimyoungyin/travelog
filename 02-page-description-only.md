# phase 2: Just client

just view, client만 작업해둡니다.

UI 디자인은 크게 신경쓰지 않으며(shadcnui 참고만), **권한 관리 등등 서버와 소통해야 하는 일은 보류합니다**.

DB를 통해 데이터를 다루어야 하는 경우는 **dummy data를 임의로 생성하여 UI를 대략 표현하도록만 합니다**.

# Pages

## `Onboarding`

-   path: "/"
-   권한: all

### content

Travelog 서비스에 대한 설명을 직관적으로 보여줍니다.

두 버튼이 보여집니다: "로그인(to /signin)" or "Travelog 구경하러 가기(to /search)"

## `Signin`

-   path: "/signin"
-   권한: unauthorized

### content

정중앙에 Card UI가 제공되어 내부에서는 소셜 로그인 버튼이 존재합니다.
social login buttons: google, github, etc..

## `Dashboard`

-   path: "/dashboard"
-   권한: authorized

### content

-   내 프로필
-   내 여행 기록 리스트: 각 리스트 요소는 컴포넌트로 처리하는데, 각각 요약 정보를 제공하고 Link로 `/travel/:id`로 연결됩니다.

## `Travel`

-   path: "/travel/:id"
-   권한: 이 travelog가 public이면 all, private면 초대된 멤버만

### content(중요)

이 페이지 뷰와 슬라이드 쇼가 이 서비스의 핵심 로직입니다.

-   컴포넌트: Sidebar(경로 상하 연결 요약, 왼쪽), DetailSidebar(지점 상세), TravelTitle(상단), GoogleMap(전체)
-   여행 경로는 state로 관리되며 sidebar와 googlemap이 이것으로 연결되어 있습니다.

#### GoogleMap

-   첫 화면

    -   먼저 Google map이 보입니다. marking되어 연결된 포인트들이 중앙에 적절한 zoom으로 클로즈업되어 보입니다.
    -   각 마커에는 기본 마커 대신 그 지점에서 찍은 사진이 있습니다. 각각은 여행 시간 순으로 연결되어 있습니다.
    -   경로들 중 첫 지점(여행 시작)은 강조되어 있습니다.

-   마커 클릭(Sidebar 지점 컴포넌트 클릭) 시(포커스 상태 변화)
    -   url params로 관리됩니다(부적절하면 다른 방안 적용 가능: state 등등)
    -   해당 마커 쪽으로 zoom이 됩니다. 또한 해당 마커는 화면 중앙에 위치합니다.

#### Sidebar

-   첫 화면

    -   좌측 사이드바에는 각 지점에 대한 컴포넌트가 여행 흐름에 따라 시간 순으로 위에서 아래로 나열되어 연결되어 있습니다.
    -   각 컴포넌트는 고정된 사이즈를 가져 y축으로 오버되면 스크롤이 가능합니다.
    -   컴포넌트는 사진 + 위치 + 제목(선택 사항) 등이 담긴 박스입니다.
    -   경로들 중 첫 지점(여행 시작)은 강조되어 있습니다.

-   마커 클릭(Sidebar 지점 컴포넌트 클릭) 시(포커스 상태 변화)
    -   sidebar의 해당 컴포넌트가 강조 처리됩니다. Sideber 우측에 DetailSidebar가 등장합니다.

#### DetailSidebar

-   첫 화면

    -   렌더링되지 않거나 Sidebar 뒤쪽에 숨겨져있습니다.

-   마커 클릭(Sidebar 지점 컴포넌트 클릭) 시(포커스 상태 변화)
    -   해당 여행 지점에 대한 상세 정보가 담겨있습니다: 사진 + 날짜 + 시간 (+ 제목 및 상세설명(작성 전엔 선택사항))
        -   제목 및 상세 설명은 권한이 있는 유저라면 수정 버튼(우측 상단 펜 모양)이 보입니다.
    -   이전 다음 버튼이 있어, 그 여행 지점으로 상태가 전환될 수 있도록 합니다.
        -   이전/다음 지점이 없으면 그 버튼은 안 보여야 할 것

#### TravelTitle

해당 Travelog의 header 역할을 하며, 상단에 고정되어 있습니다.

이 travelog의 제목을 포함한 여러 요약 설명들이 들어있습니다.

좌측 묶음: 제목 + 다녀간 국가 국기들(여러 국가 가능) + 여행 기간

우측 묶음: 좋아요 개수 아이콘 및 버튼 + 여행 슬라이드쇼 재생 버튼 + 권한 + 초대멤버만 보이는 수정 버튼

## `Show`

-   path: "/tavel/:id/show"
    -   intercepting route를 적용하여 '/travel/:id' 위에 바로 보일 수 있도록 할 수도 있고, route로 바로 접근 가능하게도 가능.
-   권한: "/travel/:id"와 동일

### content

이 서비스의 핵심 기능. 이 기능을 통해 유저가 이 여행을 돌아볼 수 있도록 합니다.

캐러셀 형태로 유저의 여행 지점을 돌아볼 수 있는 사진 쇼 개념이며, 각 지점은 사진 뿐 아니라 DetailSlidebar에 들어있는 정보가 하나의 카드처럼 담겨있습니다.

캐러셀이 좌우로 이동하면서 슬라이드하며, 중앙에 보이는 컨텐츠만 확대되어 보이고, 좌우 다음 사진은 화면 밖에 약간 걸쳐있는 정도입니다.

-   여행 지도로 돌아가기: 'travel/:id'로 이동하는 링크 버튼

-   로딩: 슬라이드쇼를 만드는 데에 시간이 걸릴 수 있으므로 '[닉네임]의 여행을 돌아보고 있어요!', '거의 다 완성됐어요!'와 같은 문구를 3초마다 번갈아 보여주며 기다릴 수 있도록 유도

-   메인 캐러셀

## `Upload`

-   path: "/travel/uplad"
-   권한: authorized

### content

이 페이지도 중요합니다. 핵심 기능을 보여주기 위한 준비 단계이기 때문이죠.

맨 위 상단에 progressbar와 같은 컴포넌트가 4조각으로 나뉘어 막대로 업로드 step 단계를 보여줍니다.

여기서도 슬라이드하듯이 캐러셀 느낌으로 각 4단계를 전환합니다

-   1단계: 여행 제목 - "여행을 한 마디로 요약하자면?"
-   2단계: 사진 업로드(최대 20장, 사진 당 최대 20MB) - 사진의 메타 데이터를 활용하여 이미지를 분석합니다.
    -   3단계 전 로딩: '사진의 메타데이터를 이용하여 [닉네임]님의 Travelog를 만들고 있습니다..
-   3단계: 메타 데이터 부족 혹은 결핍 사진을 미리보기로 보여주고 안내 - 'N개 중 M개의 사진이 정보 부족으로 travelog에서 제외되었습니다'.
    -   아래 버튼 두 개. '네 알겠습니다'(4단계로) or '다시 업로드(3단계 유지하며 사진 초기화)'
-   4단계: 로딩(show 처럼 문구 여러 개가 지나가며 업로드 기다리기) 후 - 'Travelog [제목]이/가 생성되었습니다'
    -   '생성된 travelog 보러가기' Link(/travel/:id) 버튼

## `Search`

-   path: "/search"
-   권한: all

### content

업로드 된 travelog를 검색해보는 페이지입니다.

내가 접근 가능한(Public or 내가 초대된) 여행 목록을 searchParam으로 조회합니다.

-   컴포넌트: searchbar, ListItem(dashboard에서 사용된 것 재활용)
-   무한 스크롤 가능, 최신순
